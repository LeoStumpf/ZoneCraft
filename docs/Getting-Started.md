# Getting started

## The first run

The first time ZoneCraft opens it shows a short welcome sheet — the same three-step example
that is on the [Home](Home.md) page — with two buttons: **What the buttons do** (a guide to every
control on the map, also reachable later from the layers menu) and **Start**. You can read
the welcome again any time from **Settings → Tips → Show all tips again**.

Everything stays on the phone. There is no account, and nothing is uploaded.

## Your first zone

1. Open the **layers menu** (☰, top left) and add a layer. Pick **Circles layer** — *"everything
   within a distance of a point"*. It becomes the **active layer**: the one every other
   control acts on. Its name sits in a chip on the top row.
2. Press **Add** (＋, bottom right). The map is now armed: the next tap places a circle where
   you point. Long-press Add instead to place one at the centre of the screen.
3. The circle's **editor** docks at the bottom of the screen. Set the radius there — by slider
   or by typing — while the map stays interactive behind it. Drag the circle's handle to move
   it.
4. Press the layer's **switch** (the button beside Edit, on the bottom row). On a circles
   layer that switch is **Fill outside**: the zone is now everywhere *except* the circle.
   Press it again for **Fill inside**.
5. Add a second layer for the next rule. Where the colours overlap is the area that satisfies
   both.

## How the screen is laid out

The map is full-bleed, with three groups of controls around it. [Map Controls](Map-Controls.md) names
every one of them; the short version:

- **Across the top** — the layers menu, undo and redo, the active-layer chip, and a compass
  (only while the map is rotated).
- **Up the right-hand side** — the tools: go to a place, locate me, share my location, measure
  elevation, measure distance, draw with your finger, and — only in a build pointed at a
  provider that allows it — download this area.
- **Along the bottom** — select by tapping, the layer's own switch, the imports the layer
  can take, Add, and a button that hides and shows the tools.

The bottom-left pill is the map credit; tap it for every source the map is drawn from.

## The tips

The map's buttons are icons without labels, so the first few times you press one of the
layer switches the app answers with a line saying what is now true. Each tip shows three
times and then stops. **Settings → Tips** switches them off sooner, or hands them all back.

A button that is **greyed** cannot do anything right now — an empty layer has no outside to
fill, a hidden layer shows nothing. Press it anyway: it tells you why.

## Where to next

- [Layer Types](Layer-Types.md) — what the other six kinds of layer draw.
- [Map Modes and Gestures](Map-Modes-and-Gestures.md) — selecting, editing, drawing and measuring.
- [Importing from OpenStreetMap](Importing-from-OpenStreetMap.md) — bringing real places, stations and borders into a layer.
- [Export and Backup](Export-and-Backup.md) — your map lives only on this phone; an export is the only
  copy that survives.
